{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Git Basics","text":""},{"location":"#what-is-git","title":"What is Git?","text":"<p>Git is a distributed version control system designed to handle everything from small to very large projects. However, it can also be use to register change in any kind of folder. </p>"},{"location":"#commands","title":"Commands","text":"<ul> <li><code>git init</code> - Initializes a new Git repository in the current directory.</li> <li><code>git clone</code> [URL] - Clones an existing Git repository to your computer.</li> <li><code>git add</code> [file] - Adds a specific file to the index (staging area) to be included in the next commit.</li> <li><code>git commit</code> -m \"[message]\" - Creates a new commit with the files from the index and a description message.</li> <li><code>git status</code> - Shows the current status of your working directory in relation to the repository.</li> <li><code>git branch</code> - Lists all the branches present in the repository.</li> <li><code>git checkout</code> [branch] - Switches to the specified branch.</li> <li><code>git merge</code> [branch] - Merges the changes from the specified branch into the current branch.</li> <li><code>git pull</code> - Fetches the latest changes from a remote repository into the current branch.</li> <li><code>git push</code> - Sends local changes to a remote repository.</li> <li><code>git rm</code> [file] - Removes a file from both the index and the working directory.</li> <li><code>git stash</code> - Temporarily saves uncommitted changes to switch branches.</li> </ul>"},{"location":"#extra-commands","title":"Extra CommandsProvide some additional contentThe <code>git init</code> command creates a new Git repository in the current directory.The <code>git clone</code> command creates a local copy of a remote Git repository.The <code>git add</code> command adds changes to the staging area, preparing them for a commit.The <code>git commit -m</code> command creates a commit with the staged changes and attaches a message.The <code>git status</code> command provides information on the current state of your working directory and the index.The <code>git branch</code> command shows all local branches in your repository.The <code>git checkout</code> command is used to switch between branches in your Git repository.The <code>git merge</code> command integrates changes from one branch into another.The <code>git pull</code> command combines both <code>git fetch</code> and <code>git merge</code> in one step.The <code>git push</code> command sends your commits to a remote repository, such as GitHub.","text":"<p>Git cheat sheet git-cheat-sheet-education.pdf.</p> Are you ready?Yes!No!Maybe!Submit What command is used to initialize a new Git repository?git initgit startgit newSubmit What is the command to clone a repository from a URL?git clone [URL]git copy [URL]git fetch [URL]Submit Which command adds a file to the staging area in Git?git add [file]git stage [file]git push [file]Submit How do you create a new commit with a message in Git?git commit -m \"[message]\"git save -m \"[message]\"git push -m \"[message]\"Submit Which command shows the current status of your working directory?git statusgit showgit logSubmit What command lists all branches in a repository?git branchgit listgit branchesSubmit Which command is used to switch between branches?git checkout [branch]git switch [branch]git branch [branch]Submit How do you merge changes from one branch into another?git merge [branch]git rebase [branch]git integrate [branch]Submit Which command fetches changes from a remote repository and merges them into your current branch?git pullgit fetchgit updateSubmit What is the command to send local changes to a remote repository?git pushgit sendgit uploadSubmit"},{"location":"conflict/","title":"Merge Conflicts","text":"<p>It's time to conflicts!</p>"},{"location":"git_flow/","title":"Gitflow","text":""},{"location":"git_flow/#branches","title":"Branches","text":"<ul> <li><code>master</code>: Contains the code of the current production version.</li> <li><code>develop</code>: Development branch where new features and fixes are integrated.</li> <li><code>feature</code>: Temporary branches used for developing new features.</li> <li><code>hotfix</code>: Branches created to fix critical bugs in the production version.</li> <li><code>release</code>: Branch prepared to launch a new production version, allowing for minor adjustments.</li> </ul>"},{"location":"gitcheckout/","title":"Gitcheckout Merge and rebase","text":""},{"location":"gitcheckout/#git-checkout","title":"Git checkout","text":"<p>The <code>git checkout</code> command is traditionally used to switch between different branches in a Git repository. It allows you to change the state of your local repository to reflect the content of a specific branch, a specific commit, or even a specific file at a given point in history.</p> <p>Common usage:</p> <pre><code>git checkout branch_name\n</code></pre>"},{"location":"gitcheckout/#create-a-new-branch","title":"Create a new branch","text":"<pre><code>git checkout -b branch_name\n</code></pre>"},{"location":"gitcheckout/#geting-just-one-file-from-other-branch","title":"Geting just one file from other branch","text":"<pre><code>git checkout branch_name -- example.txt\n\ngit checkout feature_del -- docs/docs/git_del.md\n</code></pre>"},{"location":"gitcheckout/#git-merge","title":"Git merge","text":"<p>The <code>git merge command</code> is used to combine changes made in two different branches. It takes the content of one branch and merges it into the branch you are currently on, creating a new commit in the process.</p> <p>Common usage:</p> <pre><code>git merge branch_name\n</code></pre> <p>But we also can use <code>git pull</code> to merge branchs.</p> <pre><code>git pull branch_name\n</code></pre>"},{"location":"gitcheckout/#git-rebase","title":"Git rebase","text":"<p>The <code>git rebase</code> command is another way to integrate changes from one branch into another. Unlike <code>git merge</code>, which combines the histories of the two branches by creating a new commit, git rebase moves or combines the series of commits to a new \"base branch\", often maintaining a linear history. </p> <p>Common usage:</p> <pre><code>git rebase base_branch_name\n</code></pre>"},{"location":"gitignore/","title":"Gitignore","text":"<p>The .gitignore file is used in Git repositories to specify which files and directories should be excluded from version control. It helps to prevent certain files from being tracked, which is particularly useful for files that are generated automatically, contain sensitive information, or are specific to your local environment. When Git encounters a file or directory listed in .gitignore, it will ignore those items when making commits or comparisons.</p> <p>Gitignore Builder Builder.</p>"},{"location":"pull_push/","title":"Git Pull and Git Push","text":""},{"location":"pull_push/#git-pull","title":"Git pull","text":"<p>The <code>git pull</code> command is used to fetch changes made in a remote repository and merge those changes into your local repository. It essentially combines the functionalities of <code>git fetch</code> and <code>git merge</code>. </p> <p><code>git fetch</code> updates local branches with updates from the remote repository but doesn't perform an automatic merge. git merge is responsible for merging those changes into the current branch. So, <code>git pull</code> performs both these operations in one step.</p> <p>Common usage:</p> <pre><code>git pull origin master\n</code></pre> <p>This command pulls changes from the master branch of the remote repository identified as origin and merges those changes into the current branch of your local repository.</p> <p>Pull example: Common usage:</p> <pre><code>Some code\n</code></pre>"},{"location":"pull_push/#git-push","title":"Git push","text":"<p>The <code>git push</code> command is used to push your local changes to a remote repository. After you've committed your changes to your local repository, you can use <code>git push</code> to share them with other team members or to store them in a remote repository for backup and history.</p> <p>Common usage:</p> <pre><code>git push origin master\n</code></pre>"},{"location":"pull_request/","title":"Pull Request","text":"<p>A Pull Request (PR) is a request to add changes from a development branch to a main branch in a Git repository. PRs are used for code review, discussion, and approval before integrating the changes. They are essential for collaboration and DevOps workflows.</p>"},{"location":"quiz/","title":"Quiz","text":"Are you ready?Yes!No!Maybe!SubmitProvide some additional content What command is used to initialize a new Git repository?git initgit startgit newSubmitThe <code>git init</code> command creates a new Git repository in the current directory. What is the command to clone a repository from a URL?git clone [URL]git copy [URL]git fetch [URL]SubmitThe <code>git clone</code> command creates a local copy of a remote Git repository. Which command adds a file to the staging area in Git?git add [file]git stage [file]git push [file]SubmitThe <code>git add</code> command adds changes to the staging area, preparing them for a commit. How do you create a new commit with a message in Git?git commit -m \"[message]\"git save -m \"[message]\"git push -m \"[message]\"SubmitThe <code>git commit -m</code> command creates a commit with the staged changes and attaches a message. Which command shows the current status of your working directory?git statusgit showgit logSubmitThe <code>git status</code> command provides information on the current state of your working directory and the index. What command lists all branches in a repository?git branchgit listgit branchesSubmitThe <code>git branch</code> command shows all local branches in your repository. Which command is used to switch between branches?git checkout [branch]git switch [branch]git branch [branch]SubmitThe <code>git checkout</code> command is used to switch between branches in your Git repository. How do you merge changes from one branch into another?git merge [branch]git rebase [branch]git integrate [branch]SubmitThe <code>git merge</code> command integrates changes from one branch into another. Which command fetches changes from a remote repository and merges them into your current branch?git pullgit fetchgit updateSubmitThe <code>git pull</code> command combines both <code>git fetch</code> and <code>git merge</code> in one step. What is the command to send local changes to a remote repository?git pushgit sendgit uploadSubmitThe <code>git push</code> command sends your commits to a remote repository, such as GitHub."}]}